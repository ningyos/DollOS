[tts]
engine = "openai-compat-tts"
base_url = "https://api.openai.com/v1"
api_key = "${OPENAI_API_KEY}"  # interpolation TBD; use a literal string or null for local endpoints
model = "tts-1"
voice = "alloy"
