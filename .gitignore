wake-up.mp3
ref-transcribe.txt
wake_word_training
.worktrees/
CubismSdkForJava-*.zip

# Python
.venv/
__pycache__/
*.egg-info/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
.coverage

# DollOS-generated data (memsearch markdown, Milvus Lite cache, etc.)
data/

# Local daemon config (copy of config.example.toml, user-specific)
config.toml

# Local discord-bridge config — holds the bot TOKEN, on-device only, never commit
# (copy of bridge.example.toml). Also cover common local/secret config variants.
bridge.toml
*.local.toml
config.*.toml
!config.example.toml

# Runtime logs
logs/

# Local agent harness state
.claude/

# Smoke / exploration audio output at repo root
/*.wav

# CodeGraph local index
.codegraph/
